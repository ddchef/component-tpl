{
  "name": "component-tpl",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "pnpm -r --parallel run start",
    "build": "pnpm -r run build",
    "publish": "pnpm run version && pnpm -r run publish",
    "version":"pnpm changeset version",
    "preinstall": "npx only-allow pnpm"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.26.2"
  }
}
